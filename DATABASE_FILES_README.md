# Database SQL Files - Ready to Copy & Paste

## 📍 File Locations

All database schema files are in the `supabase/` folder:

**Absolute Path:**
```
/Users/ibrohimrahmat/Desktop/logistics-saa-s-design (1)/supabase/
```

**Relative Path (from project root):**
```
supabase/
```

---

## 📋 Required Files (Copy These to Supabase)

### 1. CRM Schema ⭐ REQUIRED
**File Location:** `supabase/crm_schema.sql`
- Open this file
- Copy ALL contents
- Paste into Supabase SQL Editor
- Click "Run"

### 2. BOL Schema ⭐ REQUIRED
**File Location:** `supabase/bol_schema.sql`
- Open this file
- Copy ALL contents
- Paste into Supabase SQL Editor
- Click "Run"

---

## 🚀 Step-by-Step Instructions

1. **Open Supabase Dashboard**
   - Go to your Supabase project
   - Click on "SQL Editor" in the left sidebar
   - Click "New Query"

2. **Run CRM Schema**
   - Navigate to: `supabase/crm_schema.sql` in your project
   - Open the file (or I can show you the contents below)
   - Copy the entire file contents (Cmd+A, Cmd+C)
   - Paste into Supabase SQL Editor
   - Click "Run" button (or press Cmd+Enter)

3. **Run BOL Schema**
   - Navigate to: `supabase/bol_schema.sql` in your project
   - Open the file (or I can show you the contents below)
   - Copy the entire file contents (Cmd+A, Cmd+C)
   - Paste into Supabase SQL Editor
   - Click "Run" button (or press Cmd+Enter)

4. **Verify Success**
   - You should see "Success" messages for each query
   - Tables should now be created in your database

---

## 📂 How to Access Files

**Option 1: Using Cursor/VS Code**
- Look in the left sidebar file explorer
- Navigate to `supabase/` folder
- Double-click `crm_schema.sql` to open
- Copy all contents

**Option 2: Using Finder (Mac)**
- Open Finder
- Navigate to: `/Users/ibrohimrahmat/Desktop/logistics-saa-s-design (1)/supabase/`
- Double-click `crm_schema.sql` to open
- Copy all contents

---

## ⚠️ Important Notes

- **Run in order:** CRM schema first, then BOL schema
- **Copy entire file:** Make sure to copy ALL contents from each file
- **One file at a time:** Run each file separately in Supabase SQL Editor
- **If errors occur:** Check if tables already exist (that's fine, they'll be skipped)

---

The files are ready! Just open them, copy all contents, and paste into Supabase SQL Editor.


